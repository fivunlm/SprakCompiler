MAIN
    DECLARE
        (a : INT)
    ENDDECLARE

    PUT "ewr" --/ donde “ewr” debe ser una cte string /--
    GET pivot 
    PUT var1 

    a := b + 2

    IF ( a < b )
        a := 33.
    ENDIF

--/

    WHILE( a > b OR a >= b )

        --/QEQUAL( a+w/b, [(d-3)*2,e,f] )/--
        a:= a + b

    ENDWHILE

    IF ( NOT a < b )
        a := (b > 0 ? 2 : 3.)
    ENDIF

    d := .33 + 12.33

    c := "AJAJAJAJ" ++ "SAJDSAJDSAJD"
/--
ENDMAIN